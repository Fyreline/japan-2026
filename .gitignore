# ─── Private source material — never publish these ───
# Your coworker's original contains THEIR Supabase keys:
OfflineExample.html
# The Google-Sheet export has finances, flights and the 22nd surprise:
Japan Itinerary/

# macOS junk
.DS_Store
**/.DS_Store

# Node / Vite build (apps/web — ARCHITECTURE.md §2)
node_modules/
apps/web/dist/
apps/web/.env.local
.env*.local

# Python (apps/server — the login proxy, docs/AUTH.md). The real .env holds
# the service_role key, the most sensitive credential in this project — it
# must never be committed (docs/AUTH.md §4).
apps/server/.venv/
apps/server/.env
apps/server/__pycache__/
**/__pycache__/
*.pyc

# Editor / tooling
.claude/

# Temp / scratch
reference_*.json
