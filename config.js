/*
 * ─────────────────────────────────────────────────────────────
 *  Japan 2026 — site configuration
 * ─────────────────────────────────────────────────────────────
 *
 *  This is the ONLY file you need to edit to turn on shared syncing
 *  (so both of you see each other's added spots across both phones).
 *
 *  Until you fill these in, the site still works perfectly — new spots
 *  just save on the device that added them (no cross-device sharing).
 *
 *  To enable shared sync, follow SUPABASE_SETUP.md (~5 minutes, free),
 *  then paste your two values below between the quotes.
 */
window.JAPAN_CONFIG = {
  // Paste your Supabase Project URL here, e.g. 'https://abcd1234.supabase.co'
  SUPABASE_URL: 'https://sqisppizynnkbpyuqzyv.supabase.co',

  // Paste your Supabase anon (public) key here
  SUPABASE_ANON_KEY: 'sb_publishable_oi4bwATf1Er7Zl1z-_RPXA_BxDujpHD',

  // The table name (leave as-is unless you named it something else)
  SUPABASE_TABLE: 'submitted_spots'
};
